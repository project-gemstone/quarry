# description	: Displays and injects information about the toolchain and build host.

name=host-info
version=0.0.2
release=1
source=()

build() {
  msg "Preamble...\n\n"

  env

  env >$TOOLS_DIR/toolchain.env
  
  echo "TOOLCHAIN_VERSION=$version" >$TOOLS_DIR/toolchain.version

  touch $PKG/empty
}
